import React from 'react';

class PlaybackScreen extends React.Component {
  render() {
    return (
      <div className="playback-screen">
        <h2>Playback Screen</h2>
        {/* Render the video playback screen */}
      </div>
    );
  }
}

export default PlaybackScreen;
